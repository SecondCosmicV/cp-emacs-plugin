#include <bits/stdc++.h>
#define sz(x) ((ll)(x).size())
#define mod(a,b) ((((a)%(b))+(b))%(b))
using namespace std;
typedef long long ll;
const ll INF=(1LL<<62);
void solve(void){
}
int main(void){
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    ll T;
    cin>>T;
    while(T--)
        solve();
    return 0;
}

